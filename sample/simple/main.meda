// This is the main entry point for our program
func main() {
  const numPlanetsInSolarSystem: int = 8;
  const largestPlanet: string = 'Jupiter';
  const jupiterDistanceFromSun: double = 5.2; // astronomical units
  const isPlutoAPlanet: bool = false;

  printTreeNames(isPlutoAPlanet);
}
